/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { StudentsearchresultService } from './studentsearchresult.service';

describe('Service: Studentsearchresult', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [StudentsearchresultService]
    });
  });

  it('should ...', inject([StudentsearchresultService], (service: StudentsearchresultService) => {
    expect(service).toBeTruthy();
  }));
});
